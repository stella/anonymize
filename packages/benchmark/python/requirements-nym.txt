# Optional local ONNX provider for the German legal assisted lane.
# The model itself is pinned separately in nym_adapter.py.
huggingface-hub==0.34.4
numpy==2.3.2
onnxruntime==1.22.1
tokenizers==0.21.4
