---
"@stll/anonymize": patch
"@stll/anonymize-cli": patch
---

Reject lowercase organization values pulled across a newline after an institution cue, and thread CLI `--languages` into pipeline content languages so short legal-form suffixes stay scoped.
